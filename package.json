{
  "name": "birthday-flappy-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check game.js && node --check leaderboard.js"
  }
}
